name: daily_planner
description: A simple terminal-based daily planner built in Dart.
version: 1.0.0

environment:
  sdk: '>=2.18.0 <3.0.0' # Make sure this matches your Dart SDK version

dependencies:
  intl: ^0.18.1
